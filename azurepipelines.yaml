pool: Default
trigger: 
  - master
steps:
  - task: Docker@2
    inputs:  
      containerRegistry: azure to docker
      command: 'buildAndPush'
      Dockerfile: 'Kiranteja623/StudentCoursesRestAPI/Dockerfile'
      buildContext: '**'
      tags: '5.0-$(Build.BuildId)'
  - script: echo $(Build.BuildId)
